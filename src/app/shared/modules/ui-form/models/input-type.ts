export enum UIInputType {
  text = 'text',
  textNumber = 'text-number',
  number = 'number',
  select = 'select',
  checkbox = 'checkbox',
  radioButton = 'radio-button',
  textarea = 'textarea',
  date = 'date',
  time = 'time',
  switch = 'switch',
  file = 'file',
  hidden = 'hidden',
  money = 'money'
}
